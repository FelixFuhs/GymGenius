# PostgreSQL Database Connection Settings
# These values MUST match the 'environment' section for the 'db' service in docker-compose.yml
POSTGRES_USER=gymgenius
POSTGRES_PASSWORD=secret
POSTGRES_DB=gymgenius_dev
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Secret key for signing JWTs
# For local development, this can be any string.
JWT_SECRET_KEY=a-very-secret-key-that-you-should-change